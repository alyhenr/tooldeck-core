[package]
name = "tooldeck-engine"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
tooldeck-json = { path = "../tooldeck-json" }
# Add this line to enable the JS crypto feature
getrandom = { version = "0.3", features = ["wasm_js"] }